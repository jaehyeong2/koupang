server:
  port: 8088

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/koupang_test
    username: koupang_test
    password: koupang_test

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  batch:
    job:
      name:
    jdbc:
      initialize-schema: always


kakao:
  api-key: random
